# 开发流程概览

## 编译链路
1. TS -> `.gs.ts`
2. `.gs.ts` -> IR `.json`
3. IR -> `.gia`
4. 注入到地图 `.gil`

## 作用域提示
- 顶层作用域用于预计算/读文件/使用 npm。
- 节点图作用域用于编译为节点图逻辑。

## 产物用途
- `.gs.ts` 用于排查“语义差异”。
- `.json` 用于排查“节点连接与类型”。
- `.gia` 为最终注入产物。
